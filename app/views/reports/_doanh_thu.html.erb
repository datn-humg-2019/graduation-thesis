<div class="col-md-12">
  <div class="card">
    <div class="card-header">
      <%= render "filter" %>
    </div>
    <div class="card-body">
      <div class="">
        <% if params[:type].to_i.zero? %>
          <%= render "today_reports" %>
        <% else %>
          <%= render "report_with" %>
        <% end %>
      </div>
    </div>
  </div>
</div>
